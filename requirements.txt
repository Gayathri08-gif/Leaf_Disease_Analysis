# Leaf Health Check - Production Dependencies

# Frontend & Web Framework
streamlit>=1.28.1
streamlit-option-menu>=0.3.2

# Deep Learning & AI
tensorflow>=2.10.0 # TensorFlow 2.10.0 was the last version to support Python 3.7-3.10 on Windows natively. For newer Python versions (3.11+), you might need to build from source or use WSL/Linux.
keras>=2.10.0 # Keras is now integrated with TensorFlow, so align its version with TensorFlow.
numpy>=1.24.3
scikit-learn>=1.3.0

# Image Processing
opencv-python>=4.8.0 # Using >= for flexibility
Pillow>=10.0.0

# Database
# sqlite3  # Built-in with Python - no need to specify version

# Data Processing
pandas>=2.0.3
matplotlib>=3.7.2
seaborn>=0.12.2

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
Werkzeug>=2.3.7

# Development & Testing (optional, comment out for production)
pytest>=7.4.0
black>=23.7.0
pylint>=2.17.4
flake8>=6.0.0

# Deployment
gunicorn>=21.2.0
python-multipart>=0.0.6

# Google Gemini AI Integration
google-generativeai>=0.3.0

# Optional: GPU support (uncomment if using CUDA)
# tensorflow-gpu>=2.10.0
